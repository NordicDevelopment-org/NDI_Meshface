    // Core UI profile no-op stubs for omitted game/file/bot modules.
    function loadFileTransferDestinationPreference() {{
      return;
    }}

    function bindFilesControls() {{
      return;
    }}

    function renderFilesView(_state = latestState) {{
      return;
    }}

    function scheduleFileTransferMaintenance(_state = latestState) {{
      return;
    }}

    function hasActiveFileTransferSessions() {{
      return false;
    }}

    function loadReversiState() {{
      return;
    }}

    function startNewCheckersGame() {{
      return;
    }}

    function startNewChessGame() {{
      return;
    }}

    function startNewPokerGame(_options = null) {{
      return;
    }}

    function renderGamesView(_state = latestState) {{
      return;
    }}

    function bindGamesControls() {{
      return;
    }}

    function syncReversiNetworkState(_state = latestState) {{
      return;
    }}

    function syncBoardGameNetworkState(_gameId = "", _state = latestState) {{
      return;
    }}

    function loadBotRequestHistory() {{
      return;
    }}

    function loadBotTrafficSnifferCommandHeads() {{
      return;
    }}

    function bindBotHistoryControls() {{
      return;
    }}

    function bindBotsReviewControls() {{
      return;
    }}

    function renderBotsView(_state = latestState) {{
      return;
    }}

    function syncBackendBotRequestHistory(_state = latestState) {{
      return;
    }}
