# 192.168.1.87 hidden emoji grid trim comparison - 2026-06-11

Target: `http://192.168.1.87:8877/`

Before:

- Raw result: `192.168.1.87-current-iter1-2026-06-11.json`
- Result: benchmark completed, but live threshold check failed on `domTotalElements/p95`
- DOM total p95: `15,306` elements
- DOM total max: `15,349` elements
- Body HTML p95: `5,459,379` bytes
- Active surface p95: `5,147` elements
- Sample total p95: `8,730 ms`
- Callback p95: `5,910 ms`
- Poll overhead p95: `8,910 ms`
- Long tasks: `0`

After:

- Code: `e413ac7` (`Trim hidden emoji picker DOM`)
- Runtime payload hash: `1d0536b06d1746f4d43741f6ac30c109fb8b24e00b6f947201ed2042da2a888c`
- Raw result: `192.168.1.87-after-hidden-emoji-grid-trim-2026-06-11.json`
- Result: benchmark passed live thresholds
- DOM total p95: `14,977` elements
- DOM total max: `14,977` elements
- Body HTML p95: `5,418,642` bytes
- Active surface p95: `5,131` elements
- Sample total p95: `8,930 ms`
- Callback p95: `5,970.1 ms`
- Poll overhead p95: `5,711 ms`
- Long tasks: `0`

Delta:

- DOM total p95: `-329` elements (`-2.1%`)
- DOM total max: `-372` elements (`-2.4%`)
- Body HTML p95: `-40,737` bytes (`-0.7%`)
- Active surface p95: `-16` elements (`-0.3%`)
- Poll overhead p95: `-3,199 ms` (`-35.9%`)
- Sample total p95: `+200 ms` (`+2.3%`)

Notes:

- The after run had `251` visible nodes versus `261` in the before run, so small payload and marker count changes include live mesh drift.
- API timings remained healthy at roughly `10-20 ms`; API payload size does not appear to be the immediate responsiveness bottleneck for this run.
- The low-risk win was removing the hidden emoji picker button grid from the DOM until the panel is opened.
- The next larger lever is reducing the chat feed and roster render windows from `180` rows each. That would reduce DOM more, but it changes how much chat and roster content is immediately visible, so it should be treated as a UX tradeoff.
